# Data_Investigator_Agent
Data-Investigator Agent: a system that takes a plain-English business question, decides for itself how to investigate it against a real database, writes and runs its own SQL, catches and fixes its own mistakes, and only answers once it has verified the result — not when it merely outputs something.
